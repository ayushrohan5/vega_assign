

// Backend

To run backend node.js code write:

1. npm i 
2. nodemon server

create a .env file in backend and define your own: PORT = 5000, MONGO_URL, and JWT_SECRET

Project is accessed by this url:
http://localhost:5000/login
